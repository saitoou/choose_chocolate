<div class="form">
  <% @chocolates_list.each do |choco| %>
    <div class="card">
      <div class="card-name">
        <%= choco.name %>
      </div>
      <div class="card-body">
        <div>image: </div>
        <% if choco.image.blank? %>
      <img src="<%= asset_pack_path 'media/images/bar-chocolate.png' %>" width="50%" heught="50%" alt="choco"></img>
      <% else %>
      <%= choco.image %>
      <% end %>
        <div>detail: </div>
        <%= choco.detail %>
      </div>
    </div>
  <% end %>
<div>
