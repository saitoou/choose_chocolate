<div class="form">
  <% @chocolates_list.each do |choco| %>
    <div class="card">
      <div class="card-name">
        <%= choco.name %>
      </div>
      <div class="card-body">
        <div>image: </div>
        <%= choco.image %>
        <div>detail: </div>
        <%= choco.detail %>
      </div>
    </div>
  <% end %>
<div>
