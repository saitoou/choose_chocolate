<div class="form">
  <% @stress_free_choco.each do |choco| %>
  <div class="card border-0">
    <div class="card-name">
      <h1><%= choco.name %></h1>
    </div>
    <div class="card-body">
      <% if choco.image.blank? %>
        <%= image_tag 'bar-chocolate.png', id: 'checklist' , size: "350x300"%>
      <% else %>
        <%= image_tag choco.image, size: '350x220', id:'checklist' %>
      <% end %>
      <div class="md-4"></div>
      <h3><%= choco.detail %></h3>
    </div>
    <div class=result-chart>
    <div id="chart"  data-sweet-value="<%= @stress_free_review.sweet %>" data-cacao-value="<%= @stress_free_review.cacao %>" data-appearance-value="<%= @stress_free_review.appearance %>" data-texture-value="<%= @stress_free_review.texture %>" data-melt-value="<%= @stress_free_review.melt %>" ></div>
    <canvas id="myChart" width="100px" height="100px"></canvas>
  </div>
  <% end %>
  <div class="text-center">
  <%= link_to chocolates_list_path, class:'btn btn-choco-list' do %>
  <p>チョコレート一覧</p>
  <% end %>
  </div>
</div>

<%= javascript_pack_tag 'pages/result_chart' %>
